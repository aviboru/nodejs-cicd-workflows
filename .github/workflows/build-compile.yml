name: Build Compile

on:
  workflow_call:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install --prefix app
      - run: echo "npm install -g pkg"
